# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Database — SQLModel unifica SQLAlchemy + Pydantic
sqlmodel>=0.0.22
sqlalchemy>=2.0.0
psycopg[binary]>=3.1.0
alembic>=1.14.0

# Auth (se usa en Sprint 2, pero se instala ya)
python-jose[cryptography]>=3.3.0
httpx>=0.28.0

# Validation — pydantic viene con sqlmodel, pero lo declaramos explícito
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Utils
python-dotenv>=1.0.0
python-multipart>=0.0.18

# Dev / Test
pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
ruff>=0.8.0
